# 1. Dado o tamanho da base e da altura de um retângulo, calcular a sua área e o seu
# perímetro

base = int(input("qual a medida medida da base do retangulo em metros? "))
alt = int(input("qual a medida medida da altura do retangulo em metros? "))

perimetro = (base * 2) + (alt * 2)

print ("o perimetro desse retangulo eh igual a", perimetro)
